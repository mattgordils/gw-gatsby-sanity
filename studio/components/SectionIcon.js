import React from 'react'

const SectionIcon = ({ children }) => {
	return <div style={{ borderRadius: '3px', width: '40px', height: '40px', background: '#e4e8ed', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '24px' }}>{children}</div>
}

export default SectionIcon;
